subjects:
  - id: subject_a
    names: ["John Doe"]
    usernames: ["john_doe"]
    seed_urls: []
  - id: subject_b
    names: ["Jane Smith"]
    usernames: ["jane_smith"]
    seed_urls: []

search:
  platforms: ["duckduckgo_images", "instagram_public"]
  image_results_per_query: 30

face:
  provider: local_onnx_arcface
  similarity_threshold: 0.35
  # Place ONNX model at data/models/arcfaceresnet100-8.onnx
  confidence_threshold: 0.67

report:
  title: "OSINT Connection Report"
  output_dir: "outputs"

storage:
  base_dir: "data"
  keep_originals: true